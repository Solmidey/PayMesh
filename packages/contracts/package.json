{
  "name": "@paymesh/contracts",
  "version": "0.1.0",
  "scripts": {
    "build": "sh -c \"command -v forge >/dev/null 2>&1 && forge build || (echo 'forge not found, skipping build' && exit 0)\"",
    "test": "sh -c \"command -v forge >/dev/null 2>&1 && forge test || (echo 'forge not found, skipping tests' && exit 0)\""
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "typescript": "^5.9.3"
  }
}
